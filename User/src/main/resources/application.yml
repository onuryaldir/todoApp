server:
  port: 3020
spring:
  application:
    name: user
  datasource:
    password: password
    url: jdbc:postgresql://db:5432/User
    username: onuryaldir
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true