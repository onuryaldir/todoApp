server:
  port: 3000
spring:
  application:
    name: task
  datasource:
    password: password
    url: jdbc:postgresql://db:5432/Task
    username: onuryaldir
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true