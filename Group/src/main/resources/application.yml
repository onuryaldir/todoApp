server:
  port: 3010
spring:
  application:
    name: group
  datasource:
    password: password
    url: jdbc:postgresql://db:5432/Group
    username: onuryaldir
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true